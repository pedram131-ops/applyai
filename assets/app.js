const fa=n=>String(n).replace(/\d/g,d=>'۰۱۲۳۴۵۶۷۸۹'[d]);
function toggleMenu(){document.getElementById('nav').classList.toggle('open')}
function render(){let q=document.getElementById('q').value.trim().toLowerCase();let type=document.getElementById('type').value;let region=document.getElementById('region').value;let remote=document.getElementById('remote').value;let visa=document.getElementById('visa').value;let data=OPPS.filter(x=>(type==='all'||x.type===type)&&(region==='all'||x.country===region)&&(remote==='all'||x.remote===remote)&&(visa==='all'||x.visa===visa)); if(q){data=data.filter(x=>(x.title+' '+x.country+' '+x.city+' '+x.degree+' '+x.description+' '+x.requirements).toLowerCase().includes(q))}document.getElementById('totalCount').textContent=fa(data.length);document.getElementById('results').innerHTML=data.slice(0,90).map(x=>`<article class="card"><span class="tag kind">${x.typeFa}</span><span class="tag">${x.international}</span><span class="tag">${x.visa}</span><h3>${x.title}</h3><p class="meta">📍 ${x.country}، ${x.city} · ${x.source}</p><p class="meta">انتشار: ${x.gDate} / ${x.jDate} · ددلاین: ${x.deadlineG} / ${x.deadlineJ}</p><div class="details"><b>شرح کامل فارسی</b><p>${x.description}</p><b>پیش‌نیاز و مدارک</b><p>${x.requirements}</p><p><b>مدرک:</b> ${x.degree} · <b>نوع:</b> ${x.remote} · <b>اتحادیه اروپا:</b> ${x.eu}</p></div><div class="actions"><a target="_blank" href="${x.link}">منبع رسمی ↗</a><button>پیگیری</button></div></article>`).join('')||'<p>نتیجه‌ای پیدا نشد.</p>'}
document.getElementById('lastUpdate').textContent=new Date().toLocaleString('fa-IR');render();
